---
# Main tasks for Jenkins role
# Tags allow selective execution for setup or reconfiguration

- name: Install Docker
  include_tasks: tasks/install_docker.yml
  tags:
    - setup
    - docker
    - always

- name: Deploy Jenkins with Docker Compose
  include_tasks: tasks/deploy_compose.yml
  tags:
    - setup
    - deploy
    - always

- name: Configure Jenkins
  include_tasks: tasks/configure_jenkins.yml
  tags:
    - setup
    - configure
    - always
